# ResNet50-From-Scratch-Pytorch-Imagenette
Custom ResNet-50 implemented from scratch in PyTorch and trained on Imagenette. Includes full bottleneck blocks, skip connections, AMP training, Albumentations augmentation, LR scheduling, checkpointing, and a clean modular pipeline. No torchvision models used.
